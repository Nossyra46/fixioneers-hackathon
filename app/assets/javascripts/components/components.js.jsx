var Container = React.createClass({
  render: function () {
    return (
      <div>
        React test.
      </div>
    );
  }
});

// ReactDOM.render(document.getElementById('sendtext-modal'), <Container />);
